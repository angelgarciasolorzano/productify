import { afterEach, beforeAll } from "vitest";

beforeAll(() => {
  // Puedes configurar variables globales, mock de env, etc.
  // process.env.NODE_ENV = 'test';
});

afterEach(() => {
  // Limpieza entre tests si usas librerías que lo requieren.
});
